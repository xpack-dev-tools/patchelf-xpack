
import versionMajor from '@site/src/libs/versionMajor';
import versionMinor from '@site/src/libs/versionMinor';

{/* ------------------------------------------------------------------------ */}

## Release schedule

This distribution generally follows the official
NixOS PatchELF [GitHub Releases](https://github.com/NixOS/patchelf/releases/), but with a
few weeks filter, which means that releases that are overridden in
a few weeks may be skipped.
